# Fennmoor Bank - ground-truth warehouse spec

The answer key for the query-log semantic layer demo. It defines a fictional
US regional retail bank's BigQuery estate, down to every column: what each
table means, how the tables relate, and everything that is wrong with it on
purpose. A query-log generator will produce SQL against this estate. The
pipeline's job is to rediscover what is written here, and it is scored
against these files.

```bash
uv run specs/tools/build.py
```

The build expands and validates the spec, then writes everything under
`build/`. It exits non-zero on any inconsistency.

## What's in the estate

| | |
|---|---|
| Logical tables | 324 (1,169 physical once GA4 daily and Avaya monthly shards expand) |
| Projects | `fennmoor-raw` (landing), `fennmoor-dw` (dbt), `fennmoor-analytics` (legacy, BI, sandboxes) |
| Datasets | 43 |
| Source systems | Core banking (Datastream CDC), card processor, loan origination, Salesforce, Genesys, legacy Avaya, GA4, Amplitude, online banking, Braze, fraud platform, AML/KYC, credit bureau, ERP general ledger, Workday, manual reference uploads |
| dbt | 93 staging views (generated by rule), 85 hand-written intermediate/mart/ML models |
| Mess | 20 legacy EDW tables, 8 Looker PDTs, 31 team and personal sandbox tables |
| Status | 280 active, 18 frozen, 16 dead, 4 stale, 3 write-only, 3 deprecated |
| Answer keys | 15 domains / 75 subjects, 60+ concepts, 22 findings (19 planted, 3 emergent from BigQuery's cost model), 14 gold questions |

The domain is customer-centric retail banking: deposits, cards, lending,
contact center, digital, marketing, fraud, compliance and finance.

## Files

| File | Holds |
|---|---|
| `warehouse/00_estate.yaml` | Projects, datasets, teams and people, service accounts, log window and dated events, ingestion mixins, the subject taxonomy |
| `warehouse/10_raw_banking_cards_lending.yaml` | Core banking, card processor, loan origination, credit bureau |
| `warehouse/11_raw_crm_contact_center_finance.yaml` | Salesforce, Genesys, Avaya CDR, GL, Workday, reference data |
| `warehouse/12_raw_digital_marketing_risk.yaml` | GA4, Amplitude, online banking, Braze, fraud, AML |
| `warehouse/20_dw_intermediate_core.yaml` | dbt intermediate models and core marts |
| `warehouse/21_dw_marts.yaml` | Domain marts, ML features, ML scores |
| `warehouse/30_analytics.yaml` | Legacy EDW, Looker PDTs, reverse-ETL, DQ, sandboxes |
| `concepts.yaml` | Concept vocabulary (answer key for the Concept layer) and abbreviation glossary |
| `findings.yaml` | Planted findings: what's true, what the log must show, what can be detected from logs alone |
| `questions.yaml` | Gold retrieval questions: expected tables, traps, must-handle points |
| `tools/build.py` | Expander and validator (local) |
| `tools/deploy.py` | Deploys the estate empty to BigQuery and dry-runs every model (writes `build/deploy_report.md`) |
| `tools/templates.py` | Generates every query template and validates each shape by dry run (writes `build/templates.json`, `usage.json`, `TEMPLATES.md`) |
| `templates/bi.yaml` | Looker explores and dashboards, Tableau workbooks |
| `templates/human/*.yaml` | Hand-written ad-hoc SQL, one file per team |
| `simulation.yaml` | Every simulator assumption: volumes, schedules, sessions, errors, caching, cost |
| `tools/eval_navigate.py` | Grades `pipeline/navigate.py` on the 14 gold questions: traversal vs flat vs combined group choice, two table rankings (writes `build/score/NAV_SCORE.md`) |
| `tools/make_catalog.py` | Generates the synthetic data-catalog export (`designed/catalog.json`): partial coverage, stale entries, orphan terms, three wrong bindings |
| `tools/robustness.py` | Noise injection (random cross-estate queries, wrong joins) and parameter sweeps (level-1 gamma; level-2 k and gamma), against the spec and across seeds (writes `build/score/ROBUSTNESS.md`) |
| `tools/simulate.py` | Generates the JOBS log and its ground truth; `--load` puts it in BigQuery |
| `GCP_SETUP.md` | Connecting a GCP project for BigQuery dry-run validation |

## Build outputs

| File | Use |
|---|---|
| `build/warehouse.json` | Every table and column with subject, concept, status, writer, lineage and SQL |
| `build/joins.json` | Every join predicate in the models; planted wrong joins carry `trap` |
| `build/answer_key.json` | Findings and questions with fully-qualified names, for scoring |
| `build/ddl/*.sql` | Physical BigQuery DDL. **Schemas only.** |
| `build/models/**.sql` | The SELECT for every derived table |
| `build/SUMMARY.md` | Counts, identifier spellings, homonyms, PII exposure, CC ambiguity, joins |
| `build/templates.json` | Every template shape: principal, schedule, SQL with `{{params}}`, tables read and written, findings, statement type |
| `build/usage.json` | Ground truth for the window: every table's readers and writers, and whether it is live |
| `build/log/jobs.ndjson.gz` | The simulated log, one JOBS row per line |
| `build/log/jobs_truth.ndjson.gz` | Per-job truth: template, person, team, tool, session, findings, injected error |
| `build/log/SIMULATION.md` | Job mix, people, cache/error rates, cost, and the planted-finding checks |

## Design rules

**The ground truth never leaks into the warehouse.** The DDL carries no
subjects, concepts, or planted-finding notes, and table descriptions only
where a real estate plausibly has them (`bq_description`, a handful of dbt
marts). Otherwise the pipeline could read the answers out of
`INFORMATION_SCHEMA`.

**Production is correct by construction.** Every dbt model's SQL is generated
from its `from` clause and column expressions, so its lineage and joins are
exact. The builder rejects any production join across id spaces.

**Mess is deliberate and declared.** A wrong join in a human-written table
must carry `trap: <finding id>`, or the build fails. So every trap is
intentional and named in `findings.yaml`.

**Source systems keep their own conventions.** For example:

- Core banking uses `UPPER_ABBREV` names and dollars.
- The card processor uses snake case and cents, with its own `account_id`.
- Loan origination is camelCase.
- Salesforce appends `__c` to custom fields.
- GA4 has nested, sharded tables with a STRING date.

That heterogeneity is what the semantic layer has to see through.

**Honest about detectability.** Each finding says whether query logs, lineage,
schema, profiling or domain knowledge is needed to find it. Some can't be
found from logs at all (F16, F18). Those test whether the pipeline says "I
can't tell" instead of guessing.

## Column DSL

```yaml
columns:
  - "CIF_NO | STRING || customer.cif | pk required"                          # raw: no expression
  - "customer_key | INT64 | FARM_FINGERPRINT(c.cif_number) | customer.key | pk"  # derived
```

The fields are `name | TYPE | expr | concept | flags`. Flags:

- `pk`, `required`
- `pii=<kind>`, `policy=restricted`
- `unit=<cents|usd|bps|pct|sec|ms>`
- `null_after=<date>`
- `contains=<concept>@<json path>`

## Next steps

1. ~~**Validate in BigQuery.**~~ Done: all 324 tables and views deploy to
   `jeffdavis-bq-testproj` (datasets `fnb_*`), and every model's dry-run schema
   matches the spec. Re-run after any spec change; it is idempotent and bills
   nothing:

   ```bash
   uv run specs/tools/build.py && uv run specs/tools/deploy.py --project jeffdavis-bq-testproj
   ```
2. ~~**Query templates.**~~ Done: 1,742 templates / 1,793 shapes, every one
   dry-run clean in BigQuery. Code generates dbt, ingestion, legacy, Vertex,
   Looker, Tableau, Hightouch, Elementary and sandbox-CTAS SQL from the spec;
   `templates/human/*.yaml` holds 256 hand-written ad-hoc templates (307 shapes) across
   the ten teams, and `templates/bi.yaml` defines the Looker explores and
   Tableau workbooks. See `build/TEMPLATES.md`.

   ```bash
   uv run specs/tools/templates.py
   ```
3. ~~**Log simulator.**~~ Done: 315,924 jobs over 2026-04-01..06-29, in the
   exact `INFORMATION_SCHEMA.JOBS` schema, loaded into
   `jeffdavis-bq-testproj.fnb_query_log.jobs`. Behaviour is configured in
   `simulation.yaml`; ground truth per job is in
   `build/log/jobs_truth.ndjson.gz` and is never loaded into BigQuery. All
   planted-finding checks pass; see `build/log/SIMULATION.md`.

   ```bash
   uv run specs/tools/simulate.py --validate 400 --load
   ```
4. **Pipeline.** Rebuild the topology/semantic-layer pipeline against
   `fnb_query_log.jobs`, scored against `build/answer_key.json`,
   `build/usage.json` and the job-level truth.
