CREATE SCHEMA IF NOT EXISTS `fennmoor-raw.core_banking_cdc` OPTIONS (location = 'US');

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.ACCOUNT` (
  `ACCT_ID` INT64 NOT NULL,
  `ACCT_NO` STRING,
  `PROD_CD` STRING,
  `BRNCH_ID` INT64,
  `ACCT_STAT_CD` STRING,
  `OPEN_DT` DATE,
  `CLS_DT` DATE,
  `CLS_RSN_CD` STRING,
  `OPEN_CHNL_CD` STRING,
  `CCY_CD` STRING,
  `INT_RT` NUMERIC,
  `OD_LMT_AMT` NUMERIC,
  `STMT_CYC_CD` STRING,
  `ESCHEAT_FLG` STRING,
  `CRT_TS` TIMESTAMP,
  `LST_UPD_TS` TIMESTAMP,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
)
CLUSTER BY ACCT_ID;

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.ACCOUNT_BALANCE_DLY` (
  `ACCT_ID` INT64 NOT NULL,
  `BAL_DT` DATE NOT NULL,
  `LEDGER_BAL_AMT` NUMERIC,
  `AVAIL_BAL_AMT` NUMERIC,
  `COLL_BAL_AMT` NUMERIC,
  `AVG_MTD_BAL_AMT` NUMERIC,
  `ACCRD_INT_AMT` NUMERIC,
  `HOLD_AMT` NUMERIC,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
)
PARTITION BY BAL_DT
CLUSTER BY ACCT_ID;

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.ACCOUNT_CUSTOMER_REL` (
  `ACCT_ID` INT64 NOT NULL,
  `CIF_NO` STRING NOT NULL,
  `REL_TYP_CD` STRING,
  `REL_EFF_DT` DATE,
  `REL_END_DT` DATE,
  `LST_UPD_TS` TIMESTAMP,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.ACCOUNT_STATUS_HIST` (
  `ACCT_ID` INT64 NOT NULL,
  `STAT_CD` STRING,
  `PREV_STAT_CD` STRING,
  `CHG_TS` TIMESTAMP,
  `CHG_RSN_CD` STRING,
  `CHG_USER_ID` STRING,
  `CHG_CHNL_CD` STRING,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
)
PARTITION BY DATE(CHG_TS)
CLUSTER BY ACCT_ID;

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.ATM_TERMINAL` (
  `ATM_ID` STRING,
  `BRNCH_ID` INT64,
  `LOC_DESC` STRING,
  `ST_CD` STRING,
  `ZIP_CD` STRING,
  `STAT_CD` STRING,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.BRANCH` (
  `BRNCH_ID` INT64 NOT NULL,
  `BRNCH_NM` STRING,
  `BRNCH_TYP_CD` STRING,
  `ADDR_LN_1` STRING,
  `CITY_NM` STRING,
  `ST_CD` STRING,
  `ZIP_CD` STRING,
  `RGN_CD` STRING,
  `MKT_CD` STRING,
  `MGR_EMP_ID` STRING,
  `CC_ID` STRING,
  `OPEN_DT` DATE,
  `CLS_DT` DATE,
  `LAT` FLOAT64,
  `LNG` FLOAT64,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.CD_MATURITY_SCHED` (
  `ACCT_ID` INT64 NOT NULL,
  `MAT_DT` DATE,
  `TERM_MTHS` INT64,
  `RENEW_OPT_CD` STRING,
  `PRIN_AMT` NUMERIC,
  `RT` NUMERIC,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.CUSTOMER` (
  `CIF_NO` STRING NOT NULL,
  `CUST_TYP_CD` STRING,
  `FRST_NM` STRING,
  `MIDL_NM` STRING,
  `LST_NM` STRING,
  `BUS_NM` STRING,
  `TAX_ID` STRING,
  `BIRTH_DT` DATE,
  `CUST_SINCE_DT` DATE,
  `CUST_STAT_CD` STRING,
  `PREF_BRNCH_ID` INT64,
  `PREF_LANG_CD` STRING,
  `EMP_FLG` STRING,
  `SEG_CD` STRING,
  `RISK_RTNG_CD` STRING,
  `DECEASED_DT` DATE,
  `CRT_TS` TIMESTAMP,
  `LST_UPD_TS` TIMESTAMP,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
)
CLUSTER BY CIF_NO;

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.CUSTOMER_ADDRESS` (
  `CIF_NO` STRING NOT NULL,
  `ADDR_SEQ_NO` INT64,
  `ADDR_TYP_CD` STRING,
  `ADDR_LN_1` STRING,
  `ADDR_LN_2` STRING,
  `CITY_NM` STRING,
  `ST_CD` STRING,
  `ZIP_CD` STRING,
  `CNTRY_CD` STRING,
  `PRIM_FLG` STRING,
  `EFF_DT` DATE,
  `END_DT` DATE,
  `LST_UPD_TS` TIMESTAMP,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.CUSTOMER_CONTACT` (
  `CIF_NO` STRING NOT NULL,
  `CNTCT_SEQ_NO` INT64,
  `CNTCT_TYP_CD` STRING,
  `CNTCT_VAL` STRING,
  `VRFD_FLG` STRING,
  `PRIM_FLG` STRING,
  `OPT_IN_FLG` STRING,
  `LST_UPD_TS` TIMESTAMP,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.DEPOSIT_TXN` (
  `TXN_ID` INT64 NOT NULL,
  `ACCT_ID` INT64 NOT NULL,
  `POST_DT` DATE NOT NULL,
  `EFF_DT` DATE,
  `TXN_TYP_CD` STRING,
  `TXN_AMT` NUMERIC,
  `DR_CR_IND` STRING,
  `TXN_DESC` STRING,
  `CHNL_CD` STRING,
  `BRNCH_ID` INT64,
  `TELLER_ID` STRING,
  `ATM_ID` STRING,
  `ACH_TRACE_NO` STRING,
  `CHK_NO` STRING,
  `RUN_BAL_AMT` NUMERIC,
  `REVERSAL_FLG` STRING,
  `CRT_TS` TIMESTAMP,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
)
PARTITION BY POST_DT
CLUSTER BY ACCT_ID, TXN_TYP_CD;

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.FEE_ASSESSED` (
  `FEE_ID` INT64 NOT NULL,
  `ACCT_ID` INT64 NOT NULL,
  `FEE_TYP_CD` STRING,
  `FEE_AMT` NUMERIC,
  `ASSESS_DT` DATE,
  `WAIVED_FLG` STRING,
  `WAIVE_RSN_CD` STRING,
  `WAIVED_BY_USER_ID` STRING,
  `REVERSED_FLG` STRING,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
)
PARTITION BY ASSESS_DT
CLUSTER BY ACCT_ID;

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.INTEREST_ACCRUAL` (
  `ACCT_ID` INT64 NOT NULL,
  `ACCR_DT` DATE,
  `ACCR_AMT` NUMERIC,
  `RT` NUMERIC,
  `BAL_AMT` NUMERIC,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
)
PARTITION BY ACCR_DT
CLUSTER BY ACCT_ID;

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.PRODUCT` (
  `PROD_CD` STRING NOT NULL,
  `PROD_NM` STRING,
  `PROD_FMLY_CD` STRING,
  `PROD_TYP_CD` STRING,
  `MIN_BAL_AMT` NUMERIC,
  `MTHLY_FEE_AMT` NUMERIC,
  `INT_RT` NUMERIC,
  `TERM_MTHS` INT64,
  `ACTV_FLG` STRING,
  `EFF_DT` DATE,
  `END_DT` DATE,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.core_banking_cdc.STANDING_ORDER` (
  `SO_ID` INT64,
  `ACCT_ID` INT64,
  `BENE_ACCT_NO` STRING,
  `BENE_RTG_NO` STRING,
  `SO_AMT` NUMERIC,
  `FREQ_CD` STRING,
  `NEXT_EXEC_DT` DATE,
  `END_DT` DATE,
  `STAT_CD` STRING,
  `datastream_metadata` STRUCT<uuid STRING, source_timestamp INT64>
);
