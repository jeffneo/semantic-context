CREATE SCHEMA IF NOT EXISTS `fennmoor-analytics.legacy_edw` OPTIONS (location = 'US');

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.ACCT_DLY_BAL` (
  `ACCT_NBR` STRING,
  `BAL_DT` DATE,
  `LDGR_BAL_AMT` NUMERIC,
  `AVL_BAL_AMT` NUMERIC,
  `PROD_CD` STRING,
  `BRNCH_NBR` INT64,
  `LOAD_TS` TIMESTAMP
)
PARTITION BY BAL_DT;

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.ACCT_DLY_BAL_OLD` (
  `ACCT_NBR` STRING,
  `BAL_DT` DATE,
  `LDGR_BAL_AMT` NUMERIC,
  `AVL_BAL_AMT` NUMERIC,
  `PROD_CD` STRING,
  `BRNCH_NBR` INT64,
  `LOAD_TS` TIMESTAMP
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.ACCT_MSTR` (
  `ACCT_NBR` STRING,
  `CUST_ID` STRING,
  `PROD_CD` STRING,
  `BRNCH_NBR` INT64,
  `OPEN_DT` DATE,
  `CLS_DT` DATE,
  `STAT_CD` STRING,
  `CUR_BAL_AMT` NUMERIC,
  `LOAD_DT` DATE
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.BRNCH_DIM` (
  `BRNCH_NBR` INT64,
  `BRNCH_NM` STRING,
  `RGN_NM` STRING,
  `MKT_NM` STRING,
  `ST_CD` STRING,
  `OPEN_DT` DATE,
  `CLS_DT` DATE
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.CC_ACCT_MSTR` (
  `CC_ACCT_NBR` STRING,
  `CUST_ID` STRING,
  `CR_LMT_AMT` NUMERIC,
  `CUR_BAL_AMT` NUMERIC,
  `OPEN_DT` DATE,
  `CLS_DT` DATE,
  `STAT_CD` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.CC_AGENT_DLY` (
  `CALL_DT` DATE,
  `AGENT_LOGIN_ID` STRING,
  `SITE_CD` STRING,
  `CALLS_HNDLD` INT64,
  `TALK_SEC` INT64,
  `ACW_SEC` INT64,
  `AUX_SEC` INT64
)
PARTITION BY CALL_DT;

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.CC_CALL_VOL_DLY` (
  `CALL_DT` DATE,
  `SITE_CD` STRING,
  `SPLIT_SKILL` STRING,
  `CALLS_OFFRD` INT64,
  `CALLS_HNDLD` INT64,
  `CALLS_ABND` INT64,
  `AVG_TALK_SEC` NUMERIC,
  `AVG_ACW_SEC` NUMERIC,
  `SVC_LVL_PCT` NUMERIC
)
PARTITION BY CALL_DT;

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.CC_EXPNS_MTHLY` (
  `FISC_PRD` STRING,
  `CC_ID` STRING,
  `GL_ACCT_NBR` STRING,
  `ACTL_AMT` NUMERIC,
  `BDGT_AMT` NUMERIC
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.CC_TXN_HIST` (
  `CC_ACCT_NBR` STRING,
  `CUST_ID` STRING,
  `TXN_DT` DATE,
  `TXN_SEQ` INT64,
  `TXN_AMT` NUMERIC,
  `MCC_CD` STRING,
  `MRCH_NM` STRING,
  `TXN_TYP` STRING
)
PARTITION BY TXN_DT;

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.CD_RENEWAL_RPT` (
  `ACCT_NBR` STRING,
  `MAT_DT` DATE,
  `RNWL_TYP` STRING,
  `PRIN_AMT` NUMERIC,
  `RT_PCT` NUMERIC
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.CHURN_MDL_SCR` (
  `SCORE_DT` DATE,
  `CUST_ID` STRING,
  `CHURN_SCR` NUMERIC,
  `DECILE` INT64
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.CUST_ACCT_XREF` (
  `CUST_ID` STRING,
  `ACCT_NBR` STRING,
  `REL_TYP` STRING,
  `PRIM_IND` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.CUST_MSTR` (
  `CUST_ID` STRING,
  `CUST_NM` STRING,
  `CUST_TYP` STRING,
  `SSN_NBR` STRING,
  `BRTH_DT` DATE,
  `CUST_SINCE_DT` DATE,
  `SEG_CD` STRING,
  `PRIM_BRNCH_NBR` INT64,
  `HH_ID` STRING,
  `EMAIL_ADDR` STRING,
  `ZIP_CD` STRING,
  `LOAD_DT` DATE
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.CUST_MSTR_BKP_20250211` (
  `CUST_ID` STRING,
  `CUST_NM` STRING,
  `CUST_TYP` STRING,
  `SSN_NBR` STRING,
  `BRTH_DT` DATE,
  `CUST_SINCE_DT` DATE,
  `SEG_CD` STRING,
  `PRIM_BRNCH_NBR` INT64,
  `HH_ID` STRING,
  `EMAIL_ADDR` STRING,
  `ZIP_CD` STRING,
  `LOAD_DT` DATE
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.DLY_BRNCH_TXN_SUMM` (
  `BRNCH_NBR` INT64,
  `TXN_DT` DATE,
  `TXN_CNT` INT64,
  `DEP_AMT` NUMERIC,
  `WDL_AMT` NUMERIC
)
PARTITION BY TXN_DT;

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.EOM_BAL_SNAP` (
  `SNAP_MTH` DATE,
  `ACCT_NBR` STRING,
  `PROD_CD` STRING,
  `BRNCH_NBR` INT64,
  `EOM_BAL_AMT` NUMERIC
)
PARTITION BY SNAP_MTH;

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.LN_PMT_HIST` (
  `LN_NBR` STRING,
  `PMT_DT` DATE,
  `PMT_AMT` NUMERIC,
  `PRIN_AMT` NUMERIC,
  `INT_AMT` NUMERIC,
  `LATE_IND` STRING
)
PARTITION BY PMT_DT;

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.MKTG_CMPGN_RESP` (
  `CMPGN_CD` STRING,
  `CUST_ID` STRING,
  `RESP_DT` DATE,
  `RESP_TYP` STRING,
  `CHNL` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.PROD_DIM` (
  `PROD_CD` STRING,
  `PROD_NM` STRING,
  `PROD_FMLY` STRING,
  `PROD_LN` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.legacy_edw.TXN_HIST` (
  `ACCT_NBR` STRING,
  `TXN_DT` DATE,
  `TXN_SEQ` INT64,
  `TXN_AMT` NUMERIC,
  `TXN_TYP` STRING,
  `TXN_DESC` STRING,
  `CHNL` STRING
)
PARTITION BY TXN_DT;
