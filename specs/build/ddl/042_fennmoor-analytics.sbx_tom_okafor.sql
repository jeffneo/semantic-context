CREATE SCHEMA IF NOT EXISTS `fennmoor-analytics.sbx_tom_okafor` OPTIONS (location = 'US');

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.sbx_tom_okafor.tom_test` (
  `customer_key` INT64,
  `cif_number` STRING,
  `customer_type` STRING,
  `full_name` STRING,
  `birth_date` DATE,
  `tax_id_hash` STRING,
  `customer_since_date` DATE,
  `tenure_months` INT64,
  `customer_status` STRING,
  `segment` STRING,
  `preferred_branch_id` INT64,
  `preferred_language` STRING,
  `is_employee` BOOL,
  `aml_risk_rating` STRING,
  `primary_email` STRING,
  `primary_mobile` STRING,
  `email_opt_in` BOOL,
  `state_code` STRING,
  `zip_code` STRING,
  `sf_contact_id` STRING,
  `sf_household_id` STRING,
  `is_digitally_enrolled` BOOL,
  `is_deceased` BOOL
);

CREATE TABLE IF NOT EXISTS `fennmoor-analytics.sbx_tom_okafor.tom_test2` (
  `customer_key` INT64,
  `cif_number` STRING,
  `customer_type` STRING,
  `full_name` STRING,
  `customer_since_date` DATE,
  `tenure_months` INT64,
  `customer_status` STRING,
  `segment` STRING,
  `preferred_branch_id` INT64,
  `preferred_language` STRING,
  `is_employee` BOOL,
  `aml_risk_rating` STRING,
  `primary_email` STRING,
  `primary_mobile` STRING,
  `email_opt_in` BOOL,
  `state_code` STRING,
  `zip_code` STRING,
  `sf_contact_id` STRING,
  `sf_household_id` STRING,
  `is_digitally_enrolled` BOOL,
  `is_deceased` BOOL
);
