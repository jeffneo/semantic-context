CREATE SCHEMA IF NOT EXISTS `fennmoor-raw.contact_center_legacy` OPTIONS (location = 'US');

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.AGENT_ROSTER` (
  `AGENT_LOGIN_ID` STRING NOT NULL,
  `AGENT_NM` STRING,
  `EMP_ID` STRING,
  `SITE_CD` STRING,
  `TEAM_NM` STRING,
  `SUPV_NM` STRING,
  `HIRE_DT` DATE,
  `TERM_DT` DATE,
  `ACTIVE_FLG` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202301` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202302` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202303` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202304` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202305` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202306` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202307` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202308` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202309` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202310` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202311` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202312` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202401` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202402` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202403` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202404` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202405` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202406` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202407` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202408` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202409` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202410` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202411` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202412` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202501` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202502` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202503` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202504` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202505` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202506` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202507` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202508` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.cdr_202509` (
  `CALL_ID` STRING NOT NULL,
  `CALL_START_DTTM` DATETIME,
  `CALL_END_DTTM` DATETIME,
  `ANI` STRING,
  `DNIS` STRING,
  `SPLIT_SKILL` STRING,
  `AGENT_LOGIN_ID` STRING,
  `AGENT_EXT` STRING,
  `SITE_CD` STRING,
  `TALK_SEC` INT64,
  `HOLD_SEC` INT64,
  `ACW_SEC` INT64,
  `ABANDON_FLG` STRING,
  `DISPOSITION_CD` STRING,
  `TRANSFER_FLG` STRING,
  `CIF_NO` STRING
);

CREATE TABLE IF NOT EXISTS `fennmoor-raw.contact_center_legacy.SITE_LKP` (
  `SITE_CD` STRING NOT NULL,
  `SITE_DESC` STRING,
  `SITE_ST` STRING,
  `OUTSRC_FLG` STRING
);
