In this warehouse, '{abbr}' is used for: {senses}. What does {abbr} mean in the name of {object}? Decide from this table's own columns first - the ids it holds and what they are the same as - then its data sources; tables it is queried with can mix meanings.

Evidence:

{evidence}